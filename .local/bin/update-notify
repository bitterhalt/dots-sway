#!/bin/bash

set -e

cupd=$(checkupdates | wc -l)
updates=$(checkupdates | awk '{print $1}')
if [ -z "$updates" ]; then
    notify-send "No updates available"
else
    notify-send " $cupd Updates Available:" "$updates"
fi

