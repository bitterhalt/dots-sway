{
    // "layer": "top", // Waybar at top layer
    // "position": "bottom", // Waybar position (top|bottom|left|right)
     "height": 28, // Waybar height (to be removed for auto height)
    // "width": 1280, // Waybar width

    // Choose the order of the modules
    "modules-left": ["sway/workspaces", "sway/mode", "sway/scratchpad", ],
    "modules-center": ["sway/window"],
    "modules-right": ["cpu", "memory", "network", "pulseaudio", "pulseaudio#mic", "custom/swayidle", "tray", "clock", "custom/power"],

    // Modules configuration
     "sway/workspaces": {
     "disable-scroll": true,
     },

    "sway/scratchpad": {
        "format": "{icon} {count}",
        "show-empty": false,
        "format-icons": ["", "󰊠"],
        "tooltip": false,
        "tooltip-format": "{app}: {title}"
    },

    "tray": {
        // "icon-size": 14,
        "spacing": 10
    },
    "clock": {
        "format": "{:%H:%M}",
        "format-alt": "{:%a, %d.%m %H:%M}",
        "tooltip": false,
        "on-click-right": "~/.config/sway/scripts/mako_calendar",
        "on-click-middle": "foot -e calcurse",
    },

    "sway/window": {
        "max-length": 80,
        "tooltip": false

    },
    "cpu": {
        "format": "CPU: {usage}%",
        "tooltip": false,
        "on-click": "foot -a floatterm -e htop",
        "on-click-right": "~/.config/waybar/custom/usage.sh cpu",
    },

    "memory": {
        "format": "RAM: {used:0.1f}GB",
        "tooltip": false,
        "on-click": "foot -a floatterm -e htop",
        "on-click-right": "~/.config/waybar/custom/usage.sh ram",
    },

    "network": {
        // "interface": "wlp2*", // (Optional) To force the use of this interface
        "format-wifi": "({signalStrength}%) ",
        "format-ethernet": "󰈀",
        "tooltip-format": "{ifname} via {gwaddr}",
        "format-linked": "{ifname} (No IP) ?",
        "format-disconnected": "",
        "on-click": "nm-connection-editor"
    },

    "pulseaudio": {
        "format": "{icon}",
        "format-bluetooth": "{volume}% {icon}",
        "format-muted": "Mute",
        "format-icons": {
            "headphone": "",
            "headset": "󰋎",
            "default": ["", "", " "]
        },
        "on-click": "pavucontrol"
    },

    "pulseaudio#mic": {
        "format": "{format_source}",
        "format-source": "",
        "format-source-muted": "",
        "format-bluetooth": "{format_source}",
        "on-click": "pactl set-source-mute @DEFAULT_SOURCE@ toggle"
    },

    "custom/swayidle": {
        "interval": "once",
        "format": "{}",
        "exec": "~/.config/sway/scripts/swayidle-toggle.sh status",
        "signal": 8,
        "on-click": "~/.config/sway/scripts/swayidle-toggle.sh toggle",
    },

    "custom/power": {
        "interval": "once",
        "format": "",
        "tooltip": false,
        "on-click": "~/.config/sway/scripts/swaypower"
    },
}
